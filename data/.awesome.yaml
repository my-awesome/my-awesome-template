version: 1

# prefer CSV? maybe simpler?
# use underscore

data:

  # source or type?
  - type: github
    path: ./github_stars.json
    format: json
    # standard format how to parse the json? or specify fields to parse?
    # it depends from the bot
    template: "<URL>"
    enabled: true

  - type: telegram
    path: ./telegram.json
    format: json
    template: "<URL>"
    enabled: true
